const NP = 100;
let pelotas = [];

function setup() {
    createCanvas(windowWidth, windowHeight);
    rectMode(CENTER);
    for(let i = 0; i < 100; i++){
    console.log('El indice es: ' + i);
    }
}

function draw() {
    background(150);

}